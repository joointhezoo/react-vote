import React from 'react';
import {css} from '@emotion/core';

function App() {
  return (
    <div css={css`
      max-width: 720px;
      width: 100%;
      margin: 0 auto;
      padding: 24px;
      background: #eee;
    `}>
    </div>
  );
}

export default App;
